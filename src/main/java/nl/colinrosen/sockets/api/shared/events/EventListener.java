package nl.colinrosen.sockets.api.shared.events;

/**
 * @author Colin Rosen
 */
public interface EventListener {
    // Class for grouping EventListeners together. Doesn't need any methods as all events are optional
}
